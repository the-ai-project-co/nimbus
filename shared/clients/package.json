{
  "name": "@nimbus/shared-clients",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared HTTP and WebSocket clients for Nimbus",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "build": "tsc"
  },
  "dependencies": {
    "@nimbus/shared-types": "workspace:*",
    "@nimbus/shared-utils": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
